Lite Plate is a very simple CMS and it should not be considered totally secure.
--------------------------------------------------------------------------------------------------------------
To Install:
1.) copy all files into your web directory
2.) either:
-import "install_lite_plate.sql" using phpmyadmin
-or using the terminal: $ mysql -u root -p lite_plate < install_lite_plate.sql
    -this may not work on all systems; working on it...
--------------------------------------------------------------------------------------------------------------
This readme needs to be updated to match the many updates since its creation. Coming soon?